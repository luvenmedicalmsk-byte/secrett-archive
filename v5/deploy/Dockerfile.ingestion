FROM python:3.12-slim

WORKDIR /app
COPY . .

RUN pip install --no-cache-dir \
    fastapi==0.111.0 \
    uvicorn[standard]==0.30.1 \
    redis==5.0.7 \
    aiohttp==3.9.5 \
    websockets==12.0

# Default: runs hardened ingestion
CMD ["python", "workers/ingestion_hardened.py"]
