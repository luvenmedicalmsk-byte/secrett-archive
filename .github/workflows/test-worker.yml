name: Test Worker
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Copernicus Worker
        run: |
          echo "=== Test with token ==="
          curl -s -H "X-Proxy-Token: archive-proxy-2026"             "https://copernicus-proxy.luven-medical-msk.workers.dev/?action=token"
          echo ""
          echo "=== Test floods ==="
          curl -s -H "X-Proxy-Token: archive-proxy-2026"             "https://copernicus-proxy.luven-medical-msk.workers.dev/?action=floods"
          echo ""
          echo "Done"
