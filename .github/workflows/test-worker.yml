name: Test Worker
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test GDELT direct
        run: |
          echo "=== GDELT direct from GitHub Actions ==="
          curl -sv "https://api.gdeltproject.org/api/v2/doc/doc?query=war&mode=artlist&format=json&maxrecords=5&timespan=2h" 2>&1 | tail -20
